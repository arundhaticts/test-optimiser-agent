[pytest]
# Put the repo root on sys.path so tests can `from src... import ...`.
pythonpath = .
# Only treat test_* functions as tests; don't try to collect the
# TestOptimiserState TypedDict (it starts with "Test" but isn't a test class).
python_classes = _NoAutoCollect
testpaths = tests
