"""
Multi-framework test parsers.

MUST CONTAIN:
- parse(path) dispatching to per-framework parsers (start with pytest for the
  prototype; stub junit/jest/cypress). Returns the internal test representation.
"""
