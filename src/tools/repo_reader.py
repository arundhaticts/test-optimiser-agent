"""
Code/repo reader.

MUST CONTAIN:
- read_tests(path), read_source(path), detect_conventions() so generated tests
  match existing style. Used by intake + gap generation. Fatal if repo unreadable.
"""
