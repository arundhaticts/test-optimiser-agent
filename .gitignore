# Secrets — create a local .env (see docs/INSTALL.md §4); never commit the real one
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Vector store / local prototype data
.chroma/
chroma/
*.sqlite3

# Agent run artifacts (output reports, checkpointer db)
outputs/
*.checkpoint

# Uploaded test suites / criteria / CI history (POST /uploads, written by src/tools/upload_store.py)
uploads/

# Agent logs (observability — written by src/observability.py)
logs/

# Long-term memory store (per-project, written by src/memory/store.py)
.agent_memory/

# Learning artifacts
docs/demo/