# Gap generation prompt (Node 7)

MUST CONTAIN: instructions to draft new tests for given gaps, matching the project's
existing framework and conventions, each linked to its criterion/path. Return runnable
test code blocks only.
