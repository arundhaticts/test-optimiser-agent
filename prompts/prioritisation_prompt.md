# Prioritisation prompt (Node 6)

MUST CONTAIN: instructions to re-tier tests (smoke/regression/full) given the
scorecard, risk areas, and optimization_goal. Explain the weighting per goal
(speed/coverage/reliability/cost). Return structured JSON.
